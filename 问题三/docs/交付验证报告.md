# 问题三交付验证报告

## 冻结权重

三个模型使用既有原验证Macro-F1选优的完整检查点；下列哈希在复制前后均保持一致，重训冒烟实验未替换它们。

| 模型 | 轮次 | SHA256 |
|---|---:|---|
| 不确定性融合 | 5 | fe9a0e879080c4145d2d2ac55326fce2668925c9afdd78557b3b0c90a5a3af79 |
| SELF-MM适配 | 3 | f3666bf51e729796f1a7e908de8b4882ae488809d90ad5193d84896b7192e1d6 |
| TETFN源码适配 | 1 | 494afe6e7de659de649d63e79c6108141741ca8ee3d4105190495ecab45046b3 |

## 已完成的运行检查

- 从包外工作目录，设置HF_HUB_OFFLINE=1，在GPU上加载三个完整检查点，各运行附件4全部20条。ID、类别、最终强度、原始强度、三个logit与冻结输出完全一致，最大数值差0。
- 主模型输出20条原生解释，分类间隔最大核算误差5.29×10⁻⁷。CPU类别与GPU一致，最终强度最大差8.94×10⁻⁷，logit最大差2.86×10⁻⁶。
- 三个模型各使用完整3395条训练、728条验证数据跑1轮，实际执行预训练初始化、梯度更新、验证策略搜索、保存检查点；三个训练程序均成功结束，均生成三目标最佳记录。各新检查点通过predict --bundle严格离线重载并预测附件4全部20条。
- 一轮训练仅验证训练接口完整性，不替代此前20轮上限的正式实验。主模型正式运行9轮、第5轮选中；本交付没有因冒烟实验改动冻结权重。
- 评价命令用验证728条自带真值和附件4公开源标签表分别运行，结果与冻结指标一致；ID必须唯一且精确匹配，评价不重新选择策略。
- 交付测试10项通过，包括严格加载、全量附件4重放、训练参数传递、非空输出目录拒绝、模型类型校验、内部/外部标签评价。

## 文稿与解释材料核查

正式Word包含7张嵌入图片、14张表，附件4全20条解释卡齐全。DOCX压缩容器完整，主模型指标、损失和输出策略与源码及冻结文件核对一致。全部Markdown相对链接可解析。

分类贡献与回归q贡献分别报告；可靠性权重不充当贡献。语音时间标为机器对齐估计，视觉位置不能当作原特征精确帧。附件4的13号视觉无有效观测；01、13、14误判为中性，04误判为负向。正文没有把工程源码审读过程写成研究结论。

## 交付边界

完整目录含源代码、任务权重、BERT结构配置、依赖清单、源许可证、预测与解释、论文Word和生成脚本。原始比赛数据、原视频、完整CMU数据、虚拟环境和缓存不入包。完整权重以Git LFS上传；归档包含实际权重，不含LFS指针替身。

附件4公开源标签用于事后核验，20条无中性且5条与附件2测试重复。最终模型是结合既有评估和解释要求作出的探索性选择，不能称为独立新盲测。完整包超过三题合计50MB限制，尚无经本次验证符合50MB要求的等效压缩模型。

## 用户可复核的命令

在问题三目录运行：

```bash
python tools/package_delivery.py --verify
Q3B_SPECIAL="/path/to/附件4/对齐版本" HF_HUB_OFFLINE=1 python -m pytest -q -p no:cacheprovider tests/test_delivery.py
```

完整重放测试需要GPU和原始附件4对齐数据；其他测试不依赖比赛数据。核心环境为Python3.11、PyTorch2.5.1+cu124、Transformers5.15.1、NumPy2.4.6，附加测试依赖见requirements-dev.txt。

## 独立交付目录验收

从远端main建立独立工作区后，仅复制问题三目录。在包外/tmp运行交付测试，未设置原研究目录Q3B_REFERENCE，直接使用随包冻结结果进行比较：10 passed in 35.80s。三个模型全部20条推理重放、解释核算及实际代码加载均通过，证明执行不依赖旧研究源码或本机BERT缓存。
